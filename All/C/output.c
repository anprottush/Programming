#include<stdio.h>
int main()
{
    int a,b;
    printf("enter two number: ");
    scanf("%d %d",&a,&b);
if (a>b)
        maximum=a;
    else if (b>a)
        maximum=b;
          if (a<b)
        minimum=a;
    else if (b<a)
        minimum=b;


    return 0;
}
